import React, {useState, useEffect} from 'react';
import { Button, Grid } from '@mui/material';

const Gallery = () => {
 

  return (
    <Grid style={{height: '100vh'}} id="gallery">
        Gallery
    </Grid>
  );
}

export default Gallery;